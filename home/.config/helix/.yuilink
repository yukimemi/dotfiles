# Windows-only alternate location (helix reads from %APPDATA%$app).
# Parent ~/.config/helix is already covered by the home/.config marker.
[[link]]
dst = "{{ env(name='APPDATA') }}/helix"
when = "yui.os == 'windows' and vars.production"

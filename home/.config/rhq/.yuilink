# Windows-only alternate location (rhq reads from %APPDATA%$app).
# Parent ~/.config/rhq is already covered by the home/.config marker.
[[link]]
dst = "{{ env(name='APPDATA') }}/rhq"
when = "yui.os == 'windows' and vars.production"
